{
  "required": true,
  "package": "com.jkdr.abyssalascentcore.mixins",
  "refmap": "mixins.abyssalascentcore.refmap.json",
  "compatibilityLevel": "JAVA_17",
  "mixins": [
    "patch.dimchange.MixinServerTeleportationManager",
    "patch.spawn.MixinRecallSpell",
    "patch.blockbreaking.MixinServerPlayerGameModePatchForClientAndServers",
    // canReachRaw is called from handleBlockBreakAction in every environment (confirmed by disassembling the class as
    // loaded on a physical client's integrated server, not just a dedicated one), so this applies unconditionally.
    "patch.blockbreaking.MixinServerPlayerGameModePatchForDedicatedServers",

    "invokers.MixinTrunkPlacer",

    "feature.blockrules.MixinLevel",
    "feature.cfgexpansion.ironsspellbooks.MixinSpellCooldowns"
  ],
  "client": [
    "feature.blockrules.MixinMultiPlayerGameMode"
  ],
  "injectors": {
    "defaultRequire": 1
  }
}